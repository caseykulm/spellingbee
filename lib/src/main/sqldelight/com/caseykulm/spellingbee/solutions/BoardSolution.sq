CREATE TABLE boardSolution (
    board TEXT NOT NULL,
    solutions TEXT NOT NULL
);

selectAll:
SELECT *
FROM boardSolution;

selectByBoard:
SELECT *
FROM boardSolution
WHERE board = ?;

insert:
INSERT INTO boardSolution(board, solutions)
VALUES (?, ?);